package zwt.charge.domain;import lombok.Getter;import lombok.Setter;import javax.persistence.Column;import javax.persistence.Entity;import javax.persistence.GeneratedValue;import javax.persistence.GenerationType;import javax.persistence.Id;import javax.persistence.Table;import java.io.Serializable;import java.util.Date;@Entity@Table(name = "device")@Getter@Setterpublic class Device implements Serializable {    private static final long serialVersionUID = -6100144195226877008L;    @Id    @GeneratedValue(strategy = GenerationType.IDENTITY)    private Long id;    //机构id    private Long agencyId;    //设备硬件的序列号    @Column(unique = true)    private String uuid;    //设备硬件的别名    //@Column(unique = true)    private String alias;    //用户设置的设备别名    private String nickname;    //设备地址    private String address;    private Date createTime;    @Column(columnDefinition = "tinyint(4) default 0")    private boolean deleted = false;    @Column(columnDefinition = "tinyint(4) default 0")    private int type = 0;    private Integer apkVersion;    // 是否静音 true静音状态 false 非静音状态    //@Column(columnDefinition = "tinyint(4) default 0")    //private boolean mute;    // 视频状态 0 结束, 1 开始, 2 未开始    //private Long videoStatus = 2L;    @Getter    public enum Type {        STUDENT(0),        TEACHER(1);        private int id;        Type(int id) {            this.id = id;        }    }}